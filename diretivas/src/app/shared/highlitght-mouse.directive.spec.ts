import { HighlitghtMouseDirective } from './highlitght-mouse.directive';

describe('HighlitghtMouseDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlitghtMouseDirective();
    expect(directive).toBeTruthy();
  });
});
